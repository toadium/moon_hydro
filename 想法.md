# 立项申报书+全套技术文档+迭代规划完整版
## 项目全称
基于MoonBit全栈多后端架构的分布式新安江模型+浅水方程水动力耦合流域洪水智能预报系统
## 申报单位
自研技术研发团队
## 申报日期
2026年08月13日
## 完整技术总栈
核心水文内核：Rust hydro-suite 完整移植至MoonBit语言环境
后端Web框架：Proton（MoonBit官方高性能Native后端框架）
前端框架：Rabbita（原Rabbit-TEA，MoonBit官方TEA架构纯函数式前端框架）
数值计算底座：moonNum多维数组计算库、线性代数库、SIMD浮点加速
全栈架构：MoonBit Workspace多目标一体化工程，前后端共享统一领域数据模型
多编译后端：wasm-gc、wasm-wasi-gcc、native、JS四大后端全覆盖
配套依赖：sqlite3数据库绑定、Rui图表组件库、serde序列化库、async异步运行时

---

# 第一部分 立项申报书
## 一、项目立项背景、必要性与行业痛点
### 1.1 行业现状
我国山洪灾害预警、中小流域洪水预报、水库调度、水资源精细化管理，核心技术体系以**三水源新安江XAJ模型**完成流域产汇流演算，配合二维浅水方程（SWE）水动力模型完成河道洪水演进计算。当前国内行业主流实现方案主要分为三类：
1. Fortran传统水文代码：计算速率快，但缺少现代强类型约束，多流域并行仿真极易出现内存竞争、越界问题，难以搭建现代化Web平台，嵌入式终端移植、二次维护成本极高；
2. Python水文建模体系：开发门槛低，但大尺度分布式流域模拟、长序列参数自动率定存在严重性能瓶颈，实时洪水预报场景下算力不足，不适合规模化工程部署；
3. MATLAB商用水文工具箱：闭源收费、授权成本高昂，私有化部署受限，无法面向水利单位做定制化二次开发。

与此同时，传统水文预报平台普遍采用“后端Rust/Python + 前端Vue/React”前后端分离架构，存在天然短板：前后端数据模型独立编写、结构体定义不同步、接口字段频繁错位、序列化与反序列化错误频发；若需要同时支持服务器仿真、浏览器在线试算、嵌入式终端预警，必须维护多套异构代码，迭代与运维成本持续走高。

### 1.2 现有方案核心痛点
1. 仿真内核、后端服务、前端界面分属多门编程语言，跨语言调用链路复杂，调试、重构、升级难度大；
2. 水文参数率定依赖第三方工具包，SCE-UA、DDS等行业标准全局优化算法大多基于Python实现，高维参数寻优效率偏低；
3. 难以一套源码同时适配服务器高性能仿真、Wasm云函数部署、浏览器在线演算、嵌入式边缘终端四类运行环境；
4. 模型扩展能力受限，耦合水动力方程、接入AI时序预测模型、对接GIS空间平台存在较高改造门槛。

### 1.3 立项必要性
新一代编译型语言MoonBit具备静态强类型系统、极简内存模型、Trait泛型体系、Wasm/Native多后端统一编译能力，完美适配高性能科学计算与全栈Web开发场景。
本项目将成熟开源工业级Rust水文库hydro-suite完整移植至MoonBit，完成新安江模型与浅水方程双向耦合；依托Proton+Rabbita官方全栈框架，实现整套系统**单语言全栈开发、前后端共享统一数据模型**，并支持四后端编译打包，一套源代码覆盖全部部署场景。项目成果可直接服务于市县水文部门、山洪预警中心、水利科研院校、数字孪生流域项目，技术路线具备显著先进性、完备性与落地价值。

## 二、项目总体目标
### 2.1 功能目标
1. 完整移植hydro-suite整套水文模型套件至MoonBit，覆盖：三层蒸散发计算、蓄满产流机制、蓄水容量曲线、自由水三水源划分、单位线汇流演算、SCE-UA/DDS智能参数率定引擎、NSE/KGE/PBIAS水文精度评价、国标GB/T 22482洪水预报等级划分；移植完成后，数值仿真结果与原版Rust代码相对误差＜0.1%，完全兼容现行水文参数体系。
2. 在MoonBit环境内基于有限体积法完成二维浅水方程SWE求解器开发，实现流域单元产流量侧向入流、河道水动力演进、水位–地下水储量双向动态耦合仿真。
3. 基于Proton后端搭建标准化RESTful API服务，支持多流域并行模拟、多方案集合预报、批量参数自动率定、仿真方案与结果数据库持久化存储、异步任务调度。
4. 基于Rabbita+Rui组件库搭建完整Web可视化操作平台，实现参数可视化录入、降雨蒸发时序数据导入、一键启动模拟、径流过程线绘制、流域蓄水热力图渲染、仿真报表导出全套交互能力；前端编译为Wasm-GC后可在浏览器内运行轻量化新安江内核，支持免后端在线试算。
5. 实现**四大后端完整编译支持**：wasm-gc浏览器前端、wasm-wasi-gcc云函数/容器、native服务器高性能程序、JS兜底运行目标，一套源码无需修改即可完成四类产物打包部署。

### 2.2 技术目标
1. 搭建标准化MoonBit Workspace工程结构，划分shared共享模型层、backend后端层、frontend前端层，全域共用同一套水文数据结构，彻底根除传统前后端异构、接口不一致问题。
2. 性能指标：同等分布式流域网格条件下，整体仿真计算效率为Python版本的15~40倍，性能趋近原生Rust；多流域并行模拟全程线程安全、无数据竞争。
3. 跨平台兼容性：支持Windows/Linux/macOS、ARM嵌入式、RISC-V边缘终端、现代浏览器、WASI云容器全环境运行。
4. 扩展目标：统一实现HydroModel通用Trait接口，预留接入LSTM、PINN物理信息神经网络等AI水文模块的扩展接口，支持构建机理模型+数据驱动AI混合预报体系。

### 2.3 项目核心创新点
1. 国内首个完整将工业级开源新安江水文模型整套移植至MoonBit语言的耦合仿真系统，填补MoonBit高性能水文建模生态空白；
2. 采用Proton+Rabbita原生全栈架构，实现水文领域少见的单语言全栈开发+前后端共享统一数据模型架构，从底层规避接口不一致、类型错位、序列化异常等工程顽疾；
3. 完全继承hydro-suite优良架构：流域固定参数设置为不可变数据、仅瞬时仿真状态可变，高度契合MoonBit所有权与类型系统，天然支持安全并行批量模拟；
4. 一体化耦合概念性新安江产汇流模型+浅水方程河道演进模型，形成完整闭环流域水文仿真链条，优于市面上仅单一模型的预报系统；
5. 原生支持四后端多目标编译，一套工程适配前端交互、服务器集群、Wasm微服务、嵌入式预警终端多场景部署，灵活度远超传统水文软件。

## 三、项目主要开发与研究内容
### 3.1 第一阶段：hydro-suite全库MoonBit移植与数值验证
1. 模块化分步移植：hydro-core公共基础层 → hydro-xinanjiang新安江主体演算模块 → hydro-calib参数率定引擎 → hydro-topmodel辅助水文模型；
2. 数值计算适配改造：将Rust ndarray多维数组无缝替换为moonNum数组，完成广播运算、循环迭代、矩阵运算改写，核心水文公式与算法逻辑完全保持不变；
3. Trait体系迁移：复刻HydroModel通用模型特征接口，统一全部水文模型的步进仿真调用规范；
4. 测试校验：沿用原版Python参考数据集开展逐时段数值比对，确保移植后模型精度完全等效于Rust原版实现。

### 3.2 第二阶段：浅水方程求解器开发与模型双向耦合
1. 基于moonNum完成二维有限体积法SWE浅水方程离散求解器，支持地形底坡、河道糙率、上下游边界、侧向入流等完整边界条件配置；
2. 开发耦合交互接口：将新安江各单元流域产流量作为河道侧向通量边界，河道水位动态反馈调整流域地下径流储量，实现两套模型双向联动耦合；
3. 启用SIMD浮点指令集优化网格迭代效率，完成大规模河道水动力模拟性能调优。

### 3.3 第三阶段：Proton后端服务体系开发
1. 在Workspace backend目录基于Proton搭建HTTP服务，封装全套标准化API接口：
   - /api/model/run：执行单流域完整仿真，返回径流全过程序列
   - /api/calibrate/dds、/api/calibrate/sceua：启动全局参数自动率定，输出最优17项新安江参数组合
   - /api/evaluate：批量计算NSE、KGE、PBIAS指标并自动判定国标预报等级
   - /api/batch：多流域、多情景集合批量模拟任务
2. 对接SQLite数据库，实现流域基础信息、气象时序数据、仿真方案、历史结果持久化存储；
3. 开发异步任务队列、仿真进度实时推送、运行日志、异常捕获与错误处理体系。

### 3.4 第四阶段：Rabbita前端可视化平台开发
1. 前端严格遵循Rabbita标准TEA架构：全局Model状态、Msg交互消息枚举、Update状态更新逻辑、View界面渲染分层开发；
2. 依托Rui组件库搭建完整操作界面：左侧导航栏（流域管理、模型参数面板、率定算法配置、边界条件）、中部主面板（降雨蒸发时序表格编辑与导入、仿真方案配置）、右侧可视化面板（径流过程曲线、精度曲线、流域蓄水热力分布图）、底部任务管理与报表导出面板；
3. 将轻量化新安江内核编译为wasm-gc嵌入前端，实现浏览器内无需后端即可完成小型流域快速试算、方案预演。

### 3.5 第五阶段：系统联调、多后端适配、性能优化与全套文档编制
1. 全系统联调测试，验证耦合模型在多组实测流域场景下的稳定性、精度、并行运行效率；
2. 完成四后端编译适配调试，分别验证wasm-gc、wasm-wasi-gcc、native、JS四大目标产物运行正确性；
3. 完成边界条件测试、异常输入鲁棒性测试、长时间仿真稳定性测试；
4. 编制全套技术架构文档、用户操作手册、API接口文档、模型参数说明、部署运维手册、多后端打包部署指南。

## 四、项目实施周期与阶段划分（总周期6个月）
### 第1~2个月：内核移植、验证与浅水方程基础开发
完成hydro-suite整套库MoonBit完整移植、单元测试、数值精度校验；完成浅水方程基础求解器开发；搭建完整MoonBit Workspace标准化工程目录与shared共享模型层。
### 第3个月：模型耦合开发 + Proton后端完整开发
完成新安江与SWE水动力双向耦合逻辑；完成Proton全套API接口、数据库存储、异步任务调度模块；完成native后端编译调试。
### 第4个月：Rabbita前端平台完整开发 + wasm-gc前端适配
完成整套Web界面、表单绑定、图表渲染、前后端数据联动；完成Wasm前端轻量化仿真模块调试；完成wasm-gc编译目标验证。
### 第5个月：全系统联调、性能优化、四后端整体验证
并行仿真优化、SIMD加速、内存占用优化；多组实测流域数据集整体验证；完成wasm-wasi-gcc、JS剩余两类后端适配与稳定性测试；全量BUG修复，固化稳定版本。
### 第6个月：全套技术文档编制、多平台打包、验收与成果固化
完成全部技术文档、部署脚本、示范流域案例、培训材料；完成四大后端一键编译打包脚本；项目整体验收、源码归档、成果汇总。

## 五、预期成果与应用前景
### 5.1 物化预期成果
1. 一套完整开源MoonBit水文仿真内核库，遵循MIT+Apache2.0宽松开源协议，支持商用、二次开发与定制改造；
2. Proton+Rabbita一体化全栈预报系统完整MoonBit源码工程；
3. 四大后端编译产物：Native独立服务器程序、WASI容器镜像、Wasm前端网页包、JS备用运行包；
4. 全套配套技术文档集：系统架构说明书、水文模型原理手册、API接口文档、运维部署手册、参数率定操作指南、多后端编译部署教程；
5. 标准测试流域数据集、完整仿真示范案例、测试报告。

### 5.2 应用前景
1. 面向市县级水文局、山洪灾害预警中心：用于中小流域常态化洪水预报、山洪预警、水库联合优化调度；
2. 面向高校水利工程、水文水资源专业：作为新一代高性能水文教学、科研仿真平台；
3. 面向数字孪生流域项目：作为底层水文+水动力耦合仿真引擎，嵌入流域数字孪生底座，支撑实时推演与淹没模拟；
4. 可扩展接入AI时序预测模块，打造新一代“物理机理+数据驱动”混合智能水文预报体系，具备极强产业化转化与推广价值。

## 六、可行性综合分析
### 6.1 技术可行性
1. MoonBit语言在类型系统、Trait泛型、可变/不可变约束、数组运算层面大量借鉴Rust，hydro-suite整套架构与算法迁移无底层技术障碍；moonNum库完整对标Rust ndarray，数值计算逻辑可无缝替换；
2. Proton+Rabbita为MoonBit官方主推全栈开发组合，原生支持序列化绑定、跨目标编译、共享模型，前后端一体化架构为语言原生设计能力，不存在技术壁垒；
3. 新安江模型、浅水方程均为经过数十年验证的成熟水文数学模型，理论体系完备，已有大量开源实现可供交叉校验，无理论风险；
4. MoonBit官方文档、社区生态完善，多后端编译、FFI、异步运行时配套成熟，开发、调试、优化路径清晰。

### 6.2 开源合规与知识产权可行性
原hydro-suite项目采用MIT+Apache2.0双宽松开源协议，允许修改、移植、商用二次开发；MoonBit官方框架全部为开源友好协议，本项目衍生成果完全合规合法，不存在知识产权侵权风险。

### 6.3 经济与推广可行性
整套系统编译后为独立二进制程序，无需额外运行时环境，服务器部署、嵌入式部署硬件成本极低；仿真性能远超Python同类系统，算力投入更低；单语言全栈架构大幅降低后续迭代、维护、二次开发成本，商业化落地性价比优势突出。

---

# 第二部分 核心技术架构完整技术文档
## 2.1 整体MoonBit Workspace工程目录结构
```
moon_xaj_hydro/
├── moon.work                   # Workspace总配置、多后端编译目标配置、依赖锁定
├── shared/                     # 全局共享层：前后端+所有后端共用唯一一套数据模型与内核
│   ├── model.mbt               # 核心结构体：XinanjiangParams、XinanjiangState、ForcingData、SimResult、CalibConfig
│   ├── hydro_trait.mbt         # HydroModel统一通用模型特征接口
│   ├── xaj_core.mbt            # 完整移植后的新安江演算内核
│   ├── swe_core.mbt            # 二维浅水方程求解内核
│   ├── utils.mbt               # 水文通用工具、单位线卷积、评价指标计算函数
│   └── serde_bind.mbt          # 全局统一JSON序列化/反序列化绑定
├── backend/                    # Proton后端，默认编译native目标，同时支持wasm-wasi-gcc、JS目标
│   ├── main.mbt                # Proton服务入口、路由配置、中间件、跨域处理
│   ├── api_routes.mbt          # 全部RESTful接口定义与业务逻辑
│   ├── db_sqlite.mbt           # 数据库交互、数据读写、方案存储
│   └── task_scheduler.mbt      # 异步仿真任务队列、并行调度、进度推送
└── frontend/                   # Rabbita前端，默认编译wasm-gc目标，兼容JS编译目标
    ├── main.mbt                # Rabbita前端应用入口
    ├── model_bind.mbt          # 前端全局状态与shared共享模型绑定
    ├── msg_enum.mbt            # 全部前端交互消息枚举定义
    ├── update_logic.mbt        # TEA架构状态更新逻辑
    ├── view_layout.mbt         # 整体页面布局、组件排布
    ├── chart_render.mbt        # 时序曲线图、热力图渲染逻辑
    └── wasm_slim_model.mbt    # 前端轻量化仿真内核封装
```

## 2.2 四后端编译目标配置说明（moon.work核心配置）
MoonBit原生支持一套代码、多目标并行编译，在moon.work中可直接声明各子包支持的编译后端：
```toml
[workspace]
members = ["shared", "backend", "frontend"]

[package.shared]
supported_targets = ["native", "wasm-gc", "wasm-wasi-gcc", "js"]

[package.backend]
supported_targets = ["native", "wasm-wasi-gcc", "js"]
default_target = "native"

[package.frontend]
supported_targets = ["wasm-gc", "js"]
default_target = "wasm-gc"
```
### 四大后端分工与部署场景
1. **wasm-gc**
编译产物：Wasm-GC+JS网页包；部署场景：现代浏览器、WebView；用途：Rabbita前端界面、浏览器内轻量化新安江在线试算、公众预报查询页面。
2. **wasm-wasi-gcc**
编译产物：WASI标准Wasm模块；部署场景：Docker容器、云函数、Serverless、Wasm安全沙盒；用途：仿真引擎微服务部署、多租户隔离式仿真服务、轻量化云端调度。
3. **native**
编译产物：系统原生高性能二进制可执行文件；部署场景：Linux/Windows/macOS服务器、ARM/RISC-V嵌入式终端；用途：Proton主后端服务、大规模分布式并行模拟、DDS/SCE-UA批量参数率定、边缘山洪预警终端。
4. **JS目标**
编译产物：纯JavaScript代码包；部署场景：老旧浏览器、极简NodeJS环境；用途：极端环境下兜底运行、简易演示、轻量脚本调用。

shared共享模型层完全不绑定任何后端，所有结构体、算法、工具函数可在四类目标下完全复用，无需任何条件编译与代码改写。

## 2.3 前后端共享数据模型核心优势
所有水文参数、仿真状态、输入输出结构体仅在shared目录内定义唯一一份：
1. Proton后端返回JSON时自动序列化，Rabbita前端接收自动反序列化，前后端类型完全严格一致，彻底杜绝字段缺失、单位不统一、枚举定义错位等传统前后端分离顽疾；
2. 后续任意结构体字段修改、参数新增，仅修改shared内一处源码，全部后端与前端自动同步生效，无需人工维护接口文档；
3. 前端可直接调用shared内轻量化仿真函数，浏览器本地试算与后端高精度仿真使用完全同一套算法，模拟结果100%一致，不存在逻辑分叉。

## 2.4 整体模型耦合技术架构
顶层完整流程：
降雨、蒸发、地形等气象驱动数据 → 分布式单元流域新安江产汇流计算 → 各单元侧向入流汇入河道网格 → 二维浅水方程完成全河道水位、流速演进 → 出口断面流量、水位成果输出
分层架构：
1. 驱动层：气象时序数据、流域空间属性、边界约束条件
2. 概念性水文层：三水源新安江完整模型（分层蒸发、蓄满产流、自由水水源划分、坡面汇流）
3. 耦合接口层：单元产流量转换为河道侧向通量，河道水位反向修正地下水储量
4. 水动力演进层：二维SWE有限体积法求解，输出全域水位流速场
5. 评价与率定层：DDS/SCE-UA全局智能寻优 + NSE/KGE/PBIAS精度评价体系
6. 应用服务层：Proton后端API服务 + Rabbita可视化交互平台 + 四后端多场景部署

## 2.5 moon.mod.json核心依赖清单
```json
"import": [
  "moonbitlang/proton",
  "moonbitlang/rabbita",
  "moonbitlang/moonNum",
  "moonbitlang/sqlite3",
  "moonbitlang/async",
  "yoorkin/rui",
  "moonbitlang/serde"
]
```

---

# 第三部分 全版本迭代规划Roadmap
## V0.1 内部验证版本（第2个月末）
1. hydro-xinanjiang完整移植完成，可独立运行单流域时序模拟，与Rust原版结果完全对齐；
2. 一维浅水方程求解器完成基础测试；
3. Workspace工程结构定型，shared共享数据模型全部完成；
4. native后端编译验证通过。

## V0.2 后端可用版本（第3个月末）
1. Proton全套API接口开发完毕，支持仿真、参数率定、批量任务、数据库持久化；
2. 新安江与SWE双向耦合逻辑完成，小规模流域耦合仿真验证通过；
3. 后端并行模拟、异步任务调度稳定可用；
4. wasm-wasi-gcc后端编译适配完成。

## V0.3 全系统预览版本（第4个月末）
1. Rabbita完整前端界面全部开发完毕，参数面板、时序表格、径流曲线图、热力图可用；
2. 前后端数据联动完全打通，wasm-gc前端Wasm轻量化试算功能正常运行；
3. 方案管理、结果导出、基础图表功能全部完成；
4. JS备用后端初步适配完成。

## V0.4 正式稳定版本（第5个月末）
1. 全系统BUG修复、SIMD加速、内存优化、并行效率优化完成；
2. 多组实测流域数据集整体校验，预报NSE精度全部达标；
3. 四大后端全部完成稳定性测试，多操作系统一键打包脚本完成；
4. 系统异常捕获、日志系统、边界容错机制完善。

## V1.0 正式发布版本（第6个月末）
1. 全套技术架构文档、部署手册、参数指南、API文档、多后端编译教程全部定稿；
2. 完善简易权限框架、批量方案对比、多模型集合预报功能；
3. 开源代码仓库初始化，发布MoonBit水文基础模型公共库，面向社区开放使用。

## V1.1 中长期扩展迭代规划（后续6个月）
1. 接入深度学习绑定库，新增LSTM时序AI径流预测模块，构建物理机理+数据驱动混合预报模型；
2. 新增分布式集群并行调度能力，支持超大尺度流域多节点分布式仿真计算；
3. 增加GIS数据接口，支持QGIS、ArcGIS地形、水系、土地利用数据直接导入系统；
4. 扩展洪水淹没范围推演、风险区划图自动生成、调度方案优选模块；
5. 完善嵌入式RISC-V终端专用编译配置，打造低成本边缘山洪预警硬件配套方案。

---

# 第四部分 风险评估与应对方案
## 风险1：MoonBit官方框架版本迭代、接口小幅变动
应对方案：项目开发阶段锁定MoonBit稳定版本与所有依赖库固定版本号；项目架构采用模块化分层设计，仿真内核与上层Proton/Rabbita框架低耦合，即便框架小幅迭代，内核水文计算逻辑完全不受影响，升级改造工作量极低；官方Proton、Rabbita为核心维护项目，接口稳定性极高，大幅降低版本变动风险。

## 风险2：大规模多流域并行仿真性能调优工作量偏大
应对方案：Rust hydro-suite原版已具备成熟并行迭代思路，可直接迁移至MoonBit；moonNum数组原生支持分片并行遍历，配合MoonBit安全多线程模型，可快速实现安全高效并行模拟；后期可基于moonNum进一步封装专用水文并行迭代工具，持续提升超大流域仿真效率。

## 风险3：多后端编译过程中少量跨平台浮点、序列化细节差异
应对方案：项目单元测试覆盖全部四类编译目标，每次迭代自动执行四目标全量测试，提前发现并抹平跨后端细微行为差异；serde序列化库为官方标准库，在四大后端下行为高度统一，可保证数据传输一致性；数值计算使用moonNum统一浮点运算接口，跨后端计算结果完全一致。

## 风险4：水动力耦合边界条件复杂场景下稳定性调试
应对方案：分步耦合开发，先完成稳态单向耦合验证，再开发动态双向反馈耦合；预留耦合系数松弛调整参数，便于不同流域、不同河道尺度下调试稳定性；参考行业成熟浅水方程耦合水文模型案例，优化通量计算与时间步长约束机制，保障长时间仿真稳定无振荡。

## 风险5：后续AI模块接入、GIS扩展的架构扩展压力
应对方案：项目顶层统一HydroModel Trait抽象，所有水文模型、未来AI模型均遵循同一调用规范；shared共享模型层预留扩展字段，无需重构整体架构即可新增输入输出变量；整体模块化程度高，新增功能模块仅需在对应目录内新增代码，不改动现有成熟内核与前后端主体架构，扩展性极强。

# 本项目全部依赖仓库、官方框架、第三方组件完整地址清单
## 一、MoonBit 官方核心语言与基础仓库
1. MoonBit 编程语言主仓库
https://github.com/moonbitlang/moonbit
2. MoonBit 官方包管理器 Mooncakes
https://github.com/moonbitlang/mooncakes

## 二、后端框架：Proton
Proton 官方仓库（MoonBit 原生 HTTP/WebSocket 后端框架）
https://github.com/moonbitlang/proton

## 三、前端框架：Rabbita（原 Rabbit-TEA）
Rabbita 官方仓库（TEA 架构前端框架）
https://github.com/moonbitlang/rabbita

## 四、数值计算库：moonNum（对标 Rust ndarray）
moonNum 多维数组、线性代数、广播运算库
https://github.com/moonbitlang/moonNum

## 五、序列化库 serde（MoonBit 官方 JSON 序列化）
https://github.com/moonbitlang/serde

## 六、异步运行时 async
MoonBit 官方异步调度运行时
https://github.com/moonbitlang/async

## 七、SQLite3 数据库绑定
https://github.com/moonbitlang/sqlite3

## 八、前端图表与UI组件库 Rui
Rui 可视化图表、界面组件库
https://github.com/yoorkin/rui

## 九、上游移植来源：hydro-suite（Rust 原版水文内核仓库）
项目移植母体开源仓库（新安江模型、SCE-UA/DDS率定、HydroModel trait 来源）
https://github.com/virki/hydro-suite

## 十、补充配套参考仓库（可选拓展依赖）
1. MoonBit FFI C 交互工具库
https://github.com/moonbitlang/libc
2. WASI 系统接口标准库
https://github.com/moonbitlang/wasi
3. SIMD 向量加速扩展库
https://github.com/moonbitlang/simd

## 十一、可扩展备选AI/机器学习仓库（V1.1迭代规划使用）
1. MoonBit 深度学习绑定 tch-rs 封装
https://github.com/moonbitlang/torch
2. 随机数、统计工具库 stats
https://github.com/moonbitlang/stats

## 十二、开源协议汇总说明
1. hydro-suite：MIT + Apache-2.0 双协议，允许修改、移植、商用二次开发；
2. MoonBit 全部官方框架（Proton/Rabbita/moonNum/serde/sqlite3/async）：均为 Apache-2.0 开源协议；
3. Rui 组件库：MIT 协议；
整套项目所有依赖完全开源合规，无闭源、无版权限制，可完整用于立项、商用、私有化部署。

